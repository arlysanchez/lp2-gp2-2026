spring:
  application:
    name: lp2_gp2
  datasource:
    url: jdbc:oracle:thin:@localhost:1522/XE
    username: LP2
    password: admin123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql : true

server:
  servlet:
    context-path: /lp2_gp2
  port: 8082


